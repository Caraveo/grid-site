import GamingNewsPage from "@/app/news/gaming/page";
import { metadataFor } from "@/lib/seo";
import type { Metadata } from "next";

export const metadata: Metadata = metadataFor("/new/gaming");

export default GamingNewsPage;
